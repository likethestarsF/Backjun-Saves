// 240130 1 #1018
#include <iostream>
#include <string>
using namespace std;

void body() {
  // input
  short n, m;
  cin >> n >> m;

  string *str = new string[m];
  for (auto i; i < ;)

  // process

  // output
}

int main() {
  /* cin optimize */
  ios_base ::sync_with_stdio(false);
  cin.tie(NULL);
  // cout.tie(NULL);

  /* clog switch */
  // std::clog.setstate(std::ios_base::failbit);

  body();
}